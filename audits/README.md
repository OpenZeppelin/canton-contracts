# Audits

This directory contains security audit reports indexed by package name, package
version, source commit, and DAR SHA-256. Each report records its scope, auditor,
report date, and finding-resolution status, and applies only to the exact
artifacts and scope it identifies.
