# Released OpenZeppelin DARs

This directory contains the exact, checksummed production DARs attached to
OpenZeppelin GitHub Releases, organized as:

```text
<package-name>/<package-version>/<package-name>-<package-version>.dar
```
